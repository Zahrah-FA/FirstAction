export const colors = {
  black: (opacity = 1) => `rgba(0, 0, 0, ${opacity})`,
  white: '#FFFFFF',
  grey: (opacity = 1) => `rgba(128, 128, 128, ${opacity})`,
  
  // Warna merah medis utama FirstAction dengan opacity dinamis
  red: (opacity = 1) => `rgba(255, 0, 0, ${opacity})`,
};